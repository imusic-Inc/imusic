
function Reducer(state = {}, action) {
    switch (action.type) {
        case 'genre':
            
            break;
    
        default:
            break;
    }

    return state;
}

export default Reducer;